{
  "build": {
      "start": "apt update && apt install -y libgl1 && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "deploy": {
      "commands": [
          "echo 'Deployment Successful!'"
      ]
  }
}
